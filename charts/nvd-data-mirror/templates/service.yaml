apiVersion: v1
kind: Service
metadata:
  name: {{ include "nvd-data-mirror.fullname" . }}
  labels:
    {{- include "nvd-data-mirror.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nvd-data-mirror.selectorLabels" . | nindent 4 }}
